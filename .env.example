# Copy to `.env` and adjust if needed.
# Default in code: https://admin.tamtamapi.xyz (REST + live chat). Override if needed.
# VITE_API_URL=https://admin.tamtamapi.xyz
# Socket.IO only (optional; defaults to same host as REST when unset):
# VITE_LIVE_CHAT_SOCKET_URL=https://admin.tamtamapi.xyz
