# Copy to `.env` and adjust if needed.
VITE_API_URL=https://tamtamapi.xyz
# Live chat Socket.IO (default in code: https://admin.tamtamapi.xyz). Override only for local/dev.
# VITE_LIVE_CHAT_SOCKET_URL=https://admin.tamtamapi.xyz
# VITE_SOCKETIO_URL=https://admin.tamtamapi.xyz
