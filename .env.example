# Copy to `.env` and adjust if needed.
VITE_API_URL=http://Admin-backend-env.eba-9pw38gcy.us-west-2.elasticbeanstalk.com
# Optional: only if Socket.IO runs on a different origin than VITE_API_URL
# VITE_SOCKETIO_URL=
